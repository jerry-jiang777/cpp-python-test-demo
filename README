# C++-Python 测试演示项目

## 项目概述
这是一个 C++ 与 Python 集成的混合项目，主要实现了一个简单的计算器功能，使用 C++ 编写核心计算逻辑，并通过 Python 脚本进行自动化测试。

## 核心功能
- **C++ 计算器应用**: 位于 `src/main.cpp`，实现两个数字相加的功能
- **Python 测试框架**: 位于 `tests/test_runner.py`，用于自动化测试 C++ 应用
- **Docker 化部署**: 提供完整的构建和测试环境封装

## 实现的工作任务和流程

### 1. C++ 主程序功能
- 接收两个命令行参数作为输入数字
- 将字符串参数转换为双精度浮点数
- 执行加法运算
- 输出格式化的结果 (`CALCULATION_RESULT: <result>`)

### 2. 测试流程
- 使用 Python 的 `subprocess` 模块调用编译后的 C++ 程序
- 自动化运行多个测试用例（如 2+3=5, 11+4=15）
- 验证输出结果的正确性
- 提供清晰的测试通过/失败反馈

### 3. 构建系统
- 使用 CMake 进行跨平台构建管理
- `CMakeLists.txt` 配置了 C++11 标准和可执行文件生成规则

### 4. Docker 多阶段构建流程

#### 第一阶段 (Builder):
- 使用 `gcc:13` 镜像进行编译
- 安装 CMake 构建工具
- 复制源代码并编译生成 `my_app` 可执行文件

#### 第二阶段 (Runtime):
- 使用 `ubuntu:24.04` 作为运行时环境
- 复制编译好的 C++ 程序
- 安装 Python 3 和相关依赖
- 设置虚拟环境避免 PEP 668 冲突
- 运行 Python 测试脚本

### 5. CI/CD 自动化
- 配置 GitHub Actions 工作流
- 在代码推送时自动触发构建和测试流程
- 使用 Docker 构建镜像并运行测试验证

### 6. 技术特点
- **多语言集成**: C++ 核心逻辑 + Python 测试框架
- **容器化部署**: 保证环境一致性
- **自动化测试**: CI/CD 集成确保代码质量
- **现代化构建**: CMake + 多阶段 Docker 构建

## 项目价值
这个项目展示了一个完整的现代软件开发流程，包括编译、测试、打包和持续集成等环节，适合作为学习 C++ 与 Python 集成开发的示例。